sudo apt install ros-humble-xacro ros-humble-joint-state-publisher-gui

sudo apt install ros-humble-navigantion2 ros-humble-nav2-bringup  

sudo apt install ros-foxy-gazebo-ros-pkgs

sudo apt install ros-foxy-ros2-control ros-foxy-ros2-controllers ros-foxy-gazebo-ros2-control

sudo apt install python3-serial

sudo apt install ros-foxy-twist-stamper

sudo apt install xacro

git clone -b humble https://github.com/joshnewans/diffdrive_arduino.git

git clone -b humble https://github.com/joshnewans/serial.git

sudo apt remove brltty

sudo usermod -a -G dialout your-username


